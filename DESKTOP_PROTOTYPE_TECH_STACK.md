# OOTD 立体日记——本地桌面原型技术方案

> 文档版本：v1.0  
> 更新日期：2026-08-24  
> 当前阶段：本地技术与核心体验原型  
> 目标环境：单张 NVIDIA RTX 3090（24GB 显存）

## 1. 决策摘要

项目第一阶段先实现本地桌面原型，不立即开发移动端、跨平台客户端或完整云端系统。

桌面原型采用“本地 Web 界面 + 本地 Python 建模服务”的形式：用户在本机浏览器中导入照片、启动图片处理与人体/服装建模任务，并在结果页主动拖动实时模型。浏览器方案验证成功后，如确实需要可安装桌面应用，再考虑使用 Tauri 包装。

当前推荐技术组合：

- 前端：React、TypeScript、Vite、Three.js、React Three Fiber；
- 后端与建模：Python、FastAPI、PyTorch、CUDA；
- 数据：SQLite 和本地文件系统；
- 资产：GLB/glTF、静态预览图和结构化结果元数据；
- 运行环境：Ubuntu、Docker、NVIDIA Container Toolkit、RTX 3090 24GB；
- 当前不引入：React Native、Flutter、NestJS、PostgreSQL、Redis、S3、Electron、Tauri 和云端账户系统。

该原型不是完整 MVP。它优先验证项目风险最高的部分：单张全身穿搭照片能否稳定转化为用户愿意主动转动查看的立体人体/服装资产。

## 2. 原型目标

第一版需要跑通以下闭环：

```text
选择一张全身穿搭照片
        ↓
图片规范化与质量检查
        ↓
执行人物分析、服装分析和建模
        ↓
展示真实处理阶段和任务状态
        ↓
输出 GLB 模型、静态预览图和处理指标
        ↓
用户通过鼠标或触控板主动拖动模型
        ↓
对照原图、重新处理或导出结果
```

原型主要回答以下问题：

1. 单张照片能否得到可接受的人体与服装轮廓；
2. 正面向两侧转动多少角度后开始出现明显穿帮；
3. RTX 3090 完成一次端到端处理需要多长时间；
4. 失败主要发生在分割、人体拟合、表面重建、纹理还是网格后处理阶段；
5. 最终 GLB 的面数、贴图规格和文件大小能否满足实时展示；
6. 用户是否认为主动转动模型比查看普通照片更有价值。

## 3. 当前范围

### 3.1 第一版必须实现

- 从本地选择一张全身照片；
- 检查图片格式、尺寸、清晰度、人物数量和全身完整度；
- 对可尝试但不理想的照片给出警告；
- 创建异步本地任务并保存任务状态；
- 执行图片处理、人体分析、服装分析和建模管线；
- 展示真实处理阶段，不展示虚假精确百分比；
- 保存中间产物，便于定位模型问题；
- 输出 GLB/glTF 模型和静态预览图；
- 在浏览器中加载并实时渲染模型；
- 允许用户主动拖动、缩放、回正和限制角度查看；
- 支持原图与模型对照；
- 支持失败查看、重新运行和取消任务；
- 导出模型、预览图、任务日志和实验指标。

### 3.2 第一版暂不实现

- iOS 或 Android 客户端；
- React Native 或 Flutter 跨平台应用；
- 云端账户、登录和跨设备同步；
- PostgreSQL、Redis、对象存储和云端任务队列；
- 手机相机权限与拍摄引导；
- 推送通知；
- 多用户并发和商业化计费；
- 生成式生图或生成式生视频；
- 用自动旋转视频替代实时模型交互；
- 完整、准确的 360° 数字孪生；
- 正式桌面安装包。

## 4. 总体架构

```text
本地浏览器
  React / TypeScript / Three.js
        │
        │ HTTP + SSE 或 WebSocket
        ▼
FastAPI 本地服务
  ├─ 图片导入与质量检查
  ├─ 项目与任务管理
  ├─ SQLite 状态持久化
  └─ GPU Worker 调度
        │
        ▼
Python 建模管线
  ├─ 人物检测、姿态与分割
  ├─ 深度、法线和人体拟合
  ├─ 穿衣人体表面重建
  ├─ 纹理投影
  └─ 网格优化与 GLB 导出
        │
        ▼
本地工作区
  原图 / 中间结果 / GLB / 预览图 / 日志 / 指标
```

界面与建模服务应保持边界清晰。前端只消费任务状态和资产，不直接依赖某个具体模型的内部目录或输出格式。建模管线通过统一的任务输入、阶段事件和结果清单对外提供能力，以便替换候选模型。

## 5. 前端与实时模型查看器

### 5.1 推荐技术栈

- React；
- TypeScript；
- Vite；
- Three.js；
- React Three Fiber；
- Drei；
- Zustand；
- TanStack Query；
- CSS Modules 或轻量样式方案。

第一阶段直接由浏览器访问本机服务，不使用 Electron。浏览器已经能够完成照片选择、任务管理、Three.js 实时渲染和实验界面开发。

### 5.2 查看器交互

- 鼠标或触控板左右拖动模型；
- 按管线结果限制水平转动范围；
- 滚轮或手势缩放；
- 双击或按钮恢复正面；
- 松手后保持当前角度，不默认持续自动旋转；
- 模型加载前显示静态预览图；
- 模型加载失败时保留静态预览保底；
- 支持原图、纹理模型、白模、线框和法线等调试视图；
- 显示面数、贴图尺寸、资产体积、加载时间和可用转角。

### 5.3 两种界面模式

#### 用户验证模式

- 导入照片；
- 查看拍摄和图片质量提示；
- 开始建模；
- 查看处理阶段；
- 主动转动模型；
- 对照原图；
- 重新处理、删除或导出。

该模式应尽量接近未来产品的核心体验，避免展示过多技术参数。

#### 实验模式

- 选择建模管线和版本；
- 调整允许公开的实验参数；
- 查看关键点、人物蒙版和服装蒙版；
- 查看深度图和法线图；
- 查看人体基础网格和穿衣表面；
- 切换纹理、白模、线框和法线；
- 查看各阶段耗时、峰值显存和失败原因；
- 对结果进行人工评分并记录问题标签。

两种模式共用同一套任务和结果数据，通过开发模式开关控制高级功能的显示。

## 6. Python 图片处理与建模服务

### 6.1 推荐技术栈

- Python 3.10 或 3.11；
- FastAPI；
- PyTorch；
- CUDA；
- OpenCV；
- Pillow；
- NumPy；
- Open3D；
- Trimesh；
- PyMeshLab；
- PyTorch3D 或 nvdiffrast；
- Blender Headless；
- glTF Transform 或等价资产优化工具。

具体 Python、CUDA 和 PyTorch 版本在选定第一批模型后锁定。不同研究模型可能依赖不同版本的 CUDA、自定义算子和 Python 包，不应强行安装在同一个全局环境中。

### 6.2 建议的处理管线

```text
输入照片
  ↓
方向纠正、尺寸规范化与元数据清理
  ↓
图片质量检查
  ↓
人物检测与人体关键点
  ↓
人体和服装语义分割
  ↓
深度与表面法线估计
  ↓
SMPL/SMPL-X 人体基础拟合
  ↓
穿衣人体表面重建
  ↓
纹理投影与不可见区域处理
  ↓
网格补洞、清理、减面和法线修复
  ↓
GLB 导出、压缩和静态预览渲染
```

该顺序是候选结构，不要求每条实验路线包含全部步骤。每个步骤必须能够被独立替换、跳过并记录版本。

### 6.3 第一轮候选能力

| 环节 | 候选方向 |
|---|---|
| 人物与质量检查 | YOLO、MediaPipe、OpenCV |
| 人体关键点 | MMPose、RTMPose、MediaPipe Pose |
| 人体与服装解析 | SegFormer、SCHP 或服饰语义分割模型 |
| 精细前景分割 | SAM 系列作为辅助 |
| 单目深度 | Depth Anything 系列 |
| 表面法线 | DSINE 或人体法线估计模型 |
| 人体基础网格 | SMPL、SMPL-X、SMPLer-X 等路线 |
| 穿衣人体重建 | ECON、ICON、PIFuHD 等候选路线 |
| 网格后处理 | Open3D、Trimesh、PyMeshLab、Blender |
| 纹理与渲染 | PyTorch3D、nvdiffrast、Blender |

候选名称只用于技术调研，不代表已经决定采用。正式使用前必须确认模型权重、数据集和代码许可证是否允许预期用途。

### 6.4 优先比较的两条路线

#### 路线 A：完整穿衣人体网格

```text
SMPL-X 基础拟合
  → ECON/ICON/PIFu 类穿衣人体重建
  → 纹理投影
  → 网格清理与 GLB 导出
```

目标是验证完整人体与服装网格的质量上限，重点检查宽松外套、裙摆、头发、鞋包和遮挡区域。

#### 路线 B：有限角度轻量几何

```text
人物与服装分割
  → 深度/法线估计
  → 有限角度几何或分层表面
  → 纹理映射
  → 实时交互资产导出
```

目标是验证稳定、快速的 MVP 下限，接受较小的转动范围，优先保持正面观感和原图一致性。

两条路线必须使用相同测试样本和评分规则，判断复杂管线带来的质量提升是否值得其耗时和失败率。

## 7. 本地任务系统

原型仍采用异步任务模式，不能让一次 HTTP 请求持续等待全部建模步骤完成。

### 7.1 状态建议

```text
pending
preprocessing
analyzing
fitting_body
reconstructing_surface
texturing
optimizing
succeeded
failed
cancelled
```

界面展示真实阶段名称，例如“正在拟合人体”或“正在优化模型”，不根据固定计时器模拟精确百分比。

### 7.2 实现建议

- SQLite 保存项目、任务、状态、参数、阶段耗时和结果索引；
- 本地文件系统保存原图、中间产物和最终资产；
- 独立 Python Worker 进程执行 GPU 任务，避免阻塞 API 服务；
- FastAPI 负责创建、查询、取消和重试任务；
- 使用 Server-Sent Events 或 WebSocket 向前端推送状态；
- 应用或浏览器关闭后，任务状态仍然可恢复；
- 相同任务的重复操作应具有明确的重新运行语义，不能无意覆盖已有结果；
- 失败任务保留必要日志和中间产物，便于定位问题。

## 8. 本地文件与数据组织

建议的仓库结构：

```text
ootd-diorama/
  apps/
    desktop-web/             # React 桌面 Web 界面
  services/
    reconstruction-api/      # FastAPI、任务管理和 Worker
  pipelines/
    baseline-2_5d/
    smplx-reconstruction/
    clothed-human/
  packages/
    asset-schema/            # 任务与资产结构定义
  tools/
    mesh-processing/
    glb-export/
  experiments/
    manifests/
    reports/
  workspace/
    projects/
    models/
    cache/
  docs/
```

单个任务建议采用以下结构：

```text
workspace/projects/<project-id>/
  source/
    original.jpg
    normalized.png
  intermediate/
    person-mask.png
    clothing-mask.png
    keypoints.json
    depth.exr
    normal.png
    smplx.obj
    reconstructed.obj
  output/
    model.glb
    preview.png
    thumbnail.png
    result.json
  logs/
    pipeline.log
```

`workspace/`、模型权重、用户照片和大型实验资产不提交到 Git。仓库只保存脱敏的样本清单、授权状态、文件哈希、配置和实验报告。

## 9. 实验记录与评价

每次运行至少记录：

- 输入样本 ID 和授权状态；
- 管线名称和版本；
- 模型及权重版本；
- 输入、处理和导出参数；
- 每个阶段的耗时；
- 峰值 GPU 显存和系统内存；
- 成功、警告或失败状态；
- 失败代码和失败阶段；
- 最终三角面数量；
- 贴图尺寸和格式；
- GLB 文件大小；
- 浏览器加载时间和帧率；
- 建议的最大左右转动角度；
- 本人辨识度、服装还原度、表面完整度和总体观感评分。

第一批样本需要覆盖：

- 上衣加长裤；
- 长裙和短裙；
- 宽松外套；
- 深色和浅色服装；
- 复杂图案或纹理；
- 长发、卷发和头发遮挡；
- 手臂贴近身体和自然分离；
- 手持包、肩背包及明显配饰；
- 轻微遮挡、阴影和不同背景复杂度。

## 10. GLB 资产约定

桌面原型优先使用 GLB/glTF 作为实时交互资产格式。最终指标通过实验确定，初期建议关注：

- 统一坐标系、朝向和尺寸单位；
- 模型默认正面朝向一致；
- 输出合理的包围盒和相机构图元数据；
- 支持设置推荐的最小和最大水平转角；
- 网格拓扑和法线能够稳定渲染；
- 贴图不包含原图 EXIF 或位置元数据；
- 可使用 Meshopt 或 Draco 压缩几何；
- 可使用 KTX2/Basis 压缩贴图；
- 保存静态预览图作为加载和失败保底。

原型阶段不提前硬性锁定面数和文件大小，而是在不同质量档位下测量加载性能，再确定目标约束。

## 11. 环境与容器策略

- 推荐使用 Ubuntu 24.04 LTS 作为宿主环境；
- 安装与 RTX 3090 兼容的 NVIDIA 驱动；
- 使用 Docker 和 NVIDIA Container Toolkit 运行模型环境；
- 宿主机提供 GPU 驱动，容器锁定各自需要的 CUDA Runtime；
- 每条主要候选路线使用独立容器，避免 CUDA、PyTorch 和自定义算子相互冲突；
- 模型权重通过本地缓存或明确的下载脚本管理，不提交到 Git；
- 在确定第一批可用模型后，再锁定正式 Python、PyTorch、CUDA 和系统依赖版本。

推荐硬件配套：

- GPU：RTX 3090 24GB；
- 系统内存：推荐 64GB；
- 本地存储：推荐 2TB NVMe；
- 不要求多个大型模型同时驻留显存；
- 允许按阶段加载模型、使用混合精度和合理降低实验分辨率。

## 12. 为什么暂不使用 Electron 或 Tauri

### Electron

Electron 生态成熟，但会额外增加安装包、进程管理和内存占用。当前浏览器已经能够完成文件选择、任务控制和 Three.js 展示，第一阶段没有必要引入。

### Tauri

Tauri 更轻量，适合未来制作正式桌面安装包，但会提前引入 Rust、系统 WebView、进程生命周期和跨系统打包工作。算法与核心体验尚未验证前，这部分投入不能降低主要风险。

建议演进路径：

```text
阶段 1：浏览器访问 localhost
阶段 2：脚本一键启动前端、API 和 Worker
阶段 3：核心验证成功后评估 Tauri 包装
阶段 4：再决定移动端、桌面端或组合产品形态
```

## 13. 桌面原型的验证边界

### 可以验证

- 图片质量检查是否有效；
- 人体和服装建模结果是否可接受；
- 用户主动转动模型是否有产品价值；
- 单张照片适合开放的最大转动范围；
- RTX 3090 上的处理耗时和峰值显存；
- 失败率和主要失败类型；
- 模型资产大小、加载速度和渲染流畅度；
- 用户是否愿意保存、收藏或再次生成结果。

### 暂时不能完整验证

- 手机拍摄引导和相机权限；
- 手机 GPU、内存和发热表现；
- 用户每日打开应用的使用习惯；
- 手机系统分享体验；
- 弱网、云端后台任务和通知；
- 多账户、跨设备同步和线上隐私控制。

因此，桌面原型是技术可行性与核心体验验证工具，不等同于完整产品 MVP。

## 14. 推荐实施顺序

1. 建立 RTX 3090 的容器化开发环境；
2. 定义统一的任务、阶段事件和结果资产结构；
3. 创建 React/Three.js GLB 查看器；
4. 实现本地导入、SQLite 任务状态和文件工作区；
5. 接入基础质量检查、人物检测、分割和姿态估计；
6. 跑通有限角度轻量几何基线；
7. 跑通 SMPL-X 与穿衣人体重建基线；
8. 对同一批样本进行质量、耗时、显存和失败率比较；
9. 增加纹理、网格优化、GLB 压缩和预览图；
10. 完成用户验证模式和实验模式；
11. 根据实验结果锁定正式建模路线和资产规格；
12. 核心效果成立后，再讨论云部署、移动端和正式产品架构。

## 15. 第一阶段完成标准

满足以下条件后，可认为桌面原型第一阶段完成：

- 在 RTX 3090 单卡环境中可重复运行；
- 用户能够从界面导入照片并启动任务；
- 任务可查询、取消、失败后重试，并在界面重开后恢复；
- 至少有一条轻量路线和一条完整网格路线能够产生可查看结果；
- 浏览器可以加载 GLB，并由用户主动拖动查看；
- 可以限制不可靠的转动角度，且不以视频模拟交互；
- 原图、中间产物、模型、预览图、日志和指标均有清晰归档；
- 能够使用同一批样本对不同管线做可复现比较；
- 团队可以基于数据决定继续完整 3D、有限角度方案或混合方案。

## 16. 后续待定事项

- 第一批实际接入的模型与许可证；
- Python、PyTorch、CUDA 和基础镜像的锁定版本；
- 桌面原型支持 Linux 本机还是需要从其他电脑访问；
- 轻量路线的具体几何表示；
- 是否要求完整骨骼和动画能力；
- 纹理不可见区域的处理策略；
- GLB 面数、贴图、文件大小和帧率目标；
- 原型用户测试样本量和评分门槛；
- 核心验证成功后是否使用 Tauri 打包；
- 何时进入云端服务与移动端阶段。
